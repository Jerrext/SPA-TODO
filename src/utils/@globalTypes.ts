export enum ButtonType {
  PRIMARY,
  SECONDARY,
  SMALL,
}

export enum ModalWindowType {
  CreateProject,
  EditProject,
}

export enum InputType {
  TEXT = 'text',
  EMAIL = 'email',
  TELEPHONE = 'tel',
  PASSWORD = 'password',
  TEXTAREA = 'textarea',
}
